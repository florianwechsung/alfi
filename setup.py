from setuptools import setup

setup(name="Alfi",
      version="0.0.1",
      description="Augmented Lagrangian based solver for the stationary incompressible Navier-Stokes equations",
      packages = ["alfi"])
